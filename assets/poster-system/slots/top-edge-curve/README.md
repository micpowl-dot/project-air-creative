# Slot: `top-edge/curve`

**Purpose:** Single flowing line that overlays the top graphic.
**Color role:** `accent-primary`
**Format:** SVG, open stroke path. Stroke ~30–60px on 4085-wide canvas.
**Naming:** `<variant>.svg`

Replace stroke hex with `{{palette.accent-primary}}`.

## Current variants
- `ribbon.svg` — reference (single swooping curve)

## Ideas for future variants
- `double-ribbon` — two parallel curves
- `crossing-ribbon` — two ribbons that cross
- `wave-tight` — higher-frequency sine
- `straight-tilt` — straight diagonal line
