# Slot: `bg/field`

**Purpose:** Solid background field, full bleed. One layer, no texture.
**Color role:** `dominant`
**Format:** SVG (1px × 1px is fine — it stretches). Or just have n8n paint a rect.
**Naming:** `<variant>.svg`

## Current variants
- `solid.svg` — reference

## Ideas for future variants
- `noise` — very subtle film grain over the dominant
- `gradient-vertical` — dominant top, slightly darker dominant bottom
- `gradient-radial` — dominant center, darker dominant at edges
