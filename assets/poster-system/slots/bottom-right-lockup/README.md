# Slot: `bottom-right/lockup`

**Purpose:** AIR brand mark + "AI IN REACH" tagline. **Locked** — only dark/light swap allowed.
**Color role:** `anchor-dark` (dark variant) or `anchor-light` (light variant)
**Format:** SVG.
**Naming:** `air-stacked-<dark|light>.svg`

This slot exists in the system for completeness, but the lockup is not a creative slot — it is fixed brand. Do not invent new variants here.

## Current variants
- `air-stacked-dark.svg` — reference
- `air-stacked-light.svg` — planned (for when `dominant` is dark enough that anchor-dark disappears)

## Source
Master lockup files live under `../../logo/`. The files here are the locked exports for this system.
